apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: enclosed-data
  namespace: enclosed
  labels:
    app.kubernetes.io/name: enclosed
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # storageClassName: your-storage-class
